{
  "description": "migration script to new puppetserver",
  "parameters": {
    "server": {
      "description": "new server to migrate to",
      "type": "String[1]"
    }
  },
  "implementations": [
    {"name": "migration.sh", "requirements": ["shell"]},
    {"name": "migration.ps1", "requirements": ["powershell"]}
  ]
}
